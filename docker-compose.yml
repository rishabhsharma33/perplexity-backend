services:
  mongo:
    image: mongo:7
    container_name: perplexity-mongo
    restart: unless-stopped
    ports:
      - '27018:27017'
    volumes:
      - perplexity-mongo-data:/data/db

volumes:
  perplexity-mongo-data:
